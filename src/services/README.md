# services

Business-logic / API service modules live here. These wrap the lower-level
Firebase helpers in `src/lib/firebase` and expose domain-oriented functions
(e.g. `userService`, `vaultService`) consumed by stores and screens.
